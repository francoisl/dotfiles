- I use fish as my main shell. If I ask for terminal commands, you can assume I'm using fish unless I specifically mention I want something for bash or zsh
- When generating commands that filter or search data, favor using ripgrep (`rg`) over `grep`, unless asked to use grep

@RTK.md
@git-conventions.md
