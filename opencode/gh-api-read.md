Use `gh-api-get ENDPOINT [OPTIONS]` for GitHub REST API reads instead of
`gh api`. It only permits safe output options and always makes a GET request.
For GitHub API writes, use `gh api` directly and request approval.
